extra:
    beta: {}
    chartpack:
        _Chartpack: Create a new Chartpack object for plotting databank fields
    databank:
        _apply: Apply function to a selection of databank fields
        _clip: Clip all time series in databank to a new range
        _copy: Copy fields of source databank to target databank
        _eval: Evaluate an expression within a databank context
        _fieldNames: List of databank field names as a row vector of strings
        _filterFields: Get the names of databank fields that pass name or value tests
        _fromCSV: Create databank by loading CSV file
        _list: List databank fields adding date range to time series fields
        _merge: Merge two or more databanks
        _retrieveColumns: Retrieve selected columns from databank fields
        _toCSV: Write databank to CSV file
        _withEmpty: Create databank with empty time series
    dates:
        _dd: Create daily dates
        _hh: Create half-yearly dates
        _ii: Create integer dates (numbered observations)
        _mm: Create monthly dates
        _qq: Create quarterly dates
        _toString: Print IrisT dates as formatted strings
        _ww: Create weekly dates
        _yy: Create yearly dates
    dfm: {}
    dummy: {}
    explanatory:
        _regress: Estimate parameters and residual models in Explanatory object
    gamma: {}
    imf:
        _codes: Download code lists from IMF Data Portal
        _data: Download databank of time series from IMF Data Portal
        _datasets: Download list of datasets from IMF Data Portal
    linear: {}
    model: {}
    panel: {}
    plan: {}
    rephrase: {}
    series:
        _Series: Create new time series object
        _convert: Convert time series to another frequency
        _empty: Create empty time series or empty existing time series
        _fillMissing: Fill missing time series observations
        _grow: Cumulate level time series from differences or rates of growth
        _linearTrend: Create time series with linear trend
        _moving: Apply function to moving window of time series observation
        _randomlyGrowing: Create randomly growing time series
        _seasonDummy: Create time series with seasonal dummies
    slang: {}
    svar: {}
    var: {}
    x13:
        _season: Interface to X13-Arima seasonal adjustment procedure
